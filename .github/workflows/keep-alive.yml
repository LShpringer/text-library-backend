name: Keep Supabase alive

on:
  schedule:
    - cron: '0 12 * * 1'  # каждый понедельник в 12:00 UTC

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping backend
        run: curl https://text-library-backend.onrender.com/
